# Agent entry

Load `ai-copilots/README.md` first, then `ai-copilots/BOOTSTRAP.md` for module path resolution.

Quality gates: `make tidy`, `gofmt -l .`, `make vet`, `make test`, `make build`, CLI smoke via `make smoke`.
